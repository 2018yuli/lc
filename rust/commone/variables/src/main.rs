fn main() {
    /*
        let mut x = 45; // i32

        println!("The value of x is {}", x);

        // cannot assign twice to immutable variable
        x = 60;

        println!("The value of x is {}", x);
    */
    let x: i64 = 45;    // int64
    let y: u64 = 45;    // 非负
    let f: f32 = 6.7;
    let b: bool = false;

    println!("The values is {},{},{}, {}", x, y, f, b);
}
